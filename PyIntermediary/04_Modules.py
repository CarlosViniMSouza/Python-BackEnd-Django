"""
modules (as well as inheritances) are functions (objects with parameters) that
perform a certain activity when imported into the program.

PIP - Package Installer fot Python --> Is modules imports from internet by the version Python
installed in your machine (specify on compiler Python).

See a Example:
"""

import File_Hello

var = File_Hello.sayHello("Carlos")
print(var)

# See more about PiP = https://pypi.org/project/pip/